REVIEWFORGE_ADO_PAT=TODO
REVIEWFORGE_API_KEYS=TODO-generate-a-long-random-key
# Opt-in only on trusted networks: the API authenticates via X-Api-Key
# (REVIEWFORGE_API_KEYS above), and the docs surface stays open.
# ApiDocs__Enabled=false
